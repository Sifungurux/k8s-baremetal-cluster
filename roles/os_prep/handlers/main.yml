---
- name: reload sysctl
  ansible.builtin.command: sysctl --system

- name: restart kubelet
  ansible.builtin.systemd:
    name: kubelet
    state: restarted
    daemon_reload: true
  failed_when: false
