---
- name: Prepare
  hosts: all
  become: true
  tasks:
    - name: Seed a realistic swap entry in fstab (test fixture)
      ansible.builtin.lineinfile:
        path: /etc/fstab
        line: "UUID=a1b2c3d4-e5f6-47a8-89ab-cdef01234567 none swap sw 0 0"
        create: true
